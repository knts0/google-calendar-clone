import { EventModalBaseDirective } from './event-modal-base.directive'

describe('EventModalBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new EventModalBaseDirective()
    expect(directive).toBeTruthy()
  })
})
